# Unit and slice tests only — no application context boots a datasource, Redis or Event Hub here.
spring:
  application:
    name: ms-vivopay-credit-consent-bms
  flyway:
    enabled: false

eureka:
  client:
    enabled: false

lib-security:
  enabled: false
