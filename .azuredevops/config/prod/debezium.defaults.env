DEBEZIUM_NAMESPACE="${DEBEZIUM_NAMESPACE:-debezium}"
DEBEZIUM_SERVER_NAME="${DEBEZIUM_SERVER_NAME:-postgres-cdc-credit-consent-prod}"
DEBEZIUM_VERSION="${DEBEZIUM_VERSION:-3.6}"

DEBEZIUM_DATABASE_PORT="${DEBEZIUM_DATABASE_PORT:-5432}"
DEBEZIUM_DATABASE_USER="${DEBEZIUM_DATABASE_USER:-debezium}"
DEBEZIUM_PUBLICATION_NAME="${DEBEZIUM_PUBLICATION_NAME:-dbz_publication_credit_consent_prod}"
DEBEZIUM_SLOT_NAME="${DEBEZIUM_SLOT_NAME:-dbz_slot_credit_consent_prod}"
DEBEZIUM_TOPIC_PREFIX="${DEBEZIUM_TOPIC_PREFIX:-prod.debezium}"
DEBEZIUM_SCHEMA_INCLUDE_LIST="${DEBEZIUM_SCHEMA_INCLUDE_LIST:-credit_consent}"
DEBEZIUM_TABLE_INCLUDE_LIST="${DEBEZIUM_TABLE_INCLUDE_LIST:-credit_consent.tb_outbox_events}"
DEBEZIUM_SCHEMA_REGISTRY_URL="${DEBEZIUM_SCHEMA_REGISTRY_URL:-https://psrc-4j1d2.westus2.azure.confluent.cloud}"
