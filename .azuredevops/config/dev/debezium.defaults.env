DEBEZIUM_NAMESPACE="${DEBEZIUM_NAMESPACE:-debezium}"
DEBEZIUM_SERVER_NAME="${DEBEZIUM_SERVER_NAME:-postgres-cdc-credit-consent-dev}"
DEBEZIUM_VERSION="${DEBEZIUM_VERSION:-3.6}"

DEBEZIUM_DATABASE_HOST="${DEBEZIUM_DATABASE_HOST:-sql-credit-consent-test.postgres.database.azure.com}"
DEBEZIUM_DATABASE_PORT="${DEBEZIUM_DATABASE_PORT:-5432}"
DEBEZIUM_DATABASE_NAME="${DEBEZIUM_DATABASE_NAME:-db_credit_consent}"
DEBEZIUM_DATABASE_USER="${DEBEZIUM_DATABASE_USER:-debezium}"

DEBEZIUM_PUBLICATION_NAME="${DEBEZIUM_PUBLICATION_NAME:-dbz_publication_credit_consent_dev}"
DEBEZIUM_SLOT_NAME="${DEBEZIUM_SLOT_NAME:-dbz_slot_credit_consent_dev}"
DEBEZIUM_TOPIC_PREFIX="${DEBEZIUM_TOPIC_PREFIX:-dev.debezium}"
DEBEZIUM_SCHEMA_INCLUDE_LIST="${DEBEZIUM_SCHEMA_INCLUDE_LIST:-credit_consent}"
DEBEZIUM_TABLE_INCLUDE_LIST="${DEBEZIUM_TABLE_INCLUDE_LIST:-credit_consent.tb_outbox_events}"

DEBEZIUM_BOOTSTRAP_SERVERS="${DEBEZIUM_BOOTSTRAP_SERVERS:-pkc-12pd3.brazilsouth.azure.confluent.cloud:9092}"
