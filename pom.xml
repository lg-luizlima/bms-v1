<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>br.com.tlf.adft</groupId>
		<artifactId>lib-java-spring-parent-starter</artifactId>
		<version>4.1.2</version>
	</parent>


	<artifactId>ms-vivopay-credit-consent-bms-v1</artifactId>

	<version>homolog-5c11fcc8</version>

	<name>ms-vivopay-credit-consent-bms-v1</name>

	<description>O microsserviço de Consentimentos atua como um roteador dinâmico de compliance.</description>

	<properties>
		<java.version>21</java.version>
		<maven.compiler.source>21</maven.compiler.source>
		<maven.compiler.target>21</maven.compiler.target>
		<sonar.exclusions>
			**/contracts/**, **/package-info.*, **/TLFApplication.*
		</sonar.exclusions>
		<mapstruct.version>1.5.3.Final</mapstruct.version>
		<lombok-mapstruct-binding.version>0.2.0</lombok-mapstruct-binding.version>
	</properties>

	<dependencies>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-json</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
			<scope>compile</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-redis</artifactId>
			<scope>compile</scope>
		</dependency>

		<dependency>
			<groupId>com.azure</groupId>
			<artifactId>azure-messaging-eventhubs</artifactId>
			<scope>compile</scope>
		</dependency>

		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<scope>provided</scope>
			<optional>true</optional>
		</dependency>

		<dependency>
			<groupId>org.mockito</groupId>
			<artifactId>mockito-core</artifactId>
			<version>${mockito.version}</version>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>

		<!-- Observabilidade (OpenTelemetry): Micrometer Tracing (bridge OTel) + export OTLP de
		     traces/métricas, tudo gerenciado pela BOM spring-boot-dependencies herdada via
		     lib-java-spring-parent-starter — sem <version>, diferente do resilience4j. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-opentelemetry</artifactId>
		</dependency>

		<!-- Registry Prometheus para expor /actuator/prometheus (scrape local) — sem <version>,
		     gerenciado pelo micrometer-bom importado por spring-boot-dependencies. -->
		<dependency>
			<groupId>io.micrometer</groupId>
			<artifactId>micrometer-registry-prometheus</artifactId>
		</dependency>

		<!-- Appender Logback -> OTel logs (correlação log/trace no Loki via Collector).
		     Não gerenciado pela BOM do Boot — versão pinada explicitamente, como o resilience4j.
		     opentelemetry-api-incubator excluído e repinado abaixo: a versão transitiva
		     (1.55.0-alpha) é incompatível em bytecode com o opentelemetry-sdk:1.62.0 que o
		     Boot já traz (NoSuchMethodError em DeclarativeConfigProperties.get). -->
		<dependency>
			<groupId>io.opentelemetry.instrumentation</groupId>
			<artifactId>opentelemetry-logback-appender-1.0</artifactId>
			<version>2.21.0-alpha</version>
			<exclusions>
				<exclusion>
					<groupId>io.opentelemetry</groupId>
					<artifactId>opentelemetry-api-incubator</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<dependency>
			<groupId>io.opentelemetry</groupId>
			<artifactId>opentelemetry-api-incubator</artifactId>
			<version>1.62.0-alpha</version>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>io.github.openfeign</groupId>
			<artifactId>feign-jackson</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-configuration-processor</artifactId>
		</dependency>

		<dependency>
			<groupId>org.modelmapper</groupId>
			<artifactId>modelmapper</artifactId>
		</dependency>


		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-common</artifactId>
			<version>${springdoc-openapi.version}</version>
		</dependency>


		<dependency>
			<groupId>com.auth0</groupId>
			<artifactId>java-jwt</artifactId>
			<version>4.4.0</version>
		</dependency>

		<!-- spring-boot-starter-hateoas removed: pulled in spring-boot-starter-webmvc (Tomcat) transitively
		     and is otherwise unused — no HATEOAS APIs (EntityModel/WebMvcLinkBuilder/etc.) anywhere in this
		     codebase. spring-boot-starter-webflux also removed: WebClientConfiguration's outbound calls now
		     go through a blocking RestClient (org.apache.hc.client5 HttpComponents), matching the rest of
		     this app's synchronous request path — no reactive dependency needed. -->

		<!-- PostgreSQL / JPA -->

		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>

		<!-- Spring Boot 4.x split Flyway's autoconfiguration out of the core autoconfigure jar into its
		     own module — flyway-core alone is not enough to activate FlywayAutoConfiguration. Schema is
		     managed by Flyway, not Hibernate ddl-auto (which is set to "validate" only). -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-flyway</artifactId>
		</dependency>

		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-core</artifactId>
		</dependency>

		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
		</dependency>


		<dependency>
			<groupId>org.springframework.data</groupId>
			<artifactId>spring-data-redis</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-autoconfigure</artifactId>
		</dependency>

		<dependency>
			<groupId>org.mapstruct</groupId>
			<artifactId>mapstruct</artifactId>
			<version>${mapstruct.version}</version>
		</dependency>

		<dependency>
			<groupId>org.mapstruct</groupId>
			<artifactId>mapstruct-processor</artifactId>
			<version>${mapstruct.version}</version>
		</dependency>

		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok-mapstruct-binding</artifactId>
			<version>${lombok-mapstruct-binding.version}</version>
		</dependency>

		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>0.11.5</version>
		</dependency>

		<dependency>
			<groupId>br.com.tlf.vvpp</groupId>
			<artifactId>lib-fintech-log</artifactId>
			<version>2.0.1</version>
		</dependency>

		<!-- Spans CLIENT por conexão/query JDBC (Postgres), no mesmo padrão do que o Spring
		     Data Redis já faz sozinho para o Lettuce. O jar oficial 1.1.1 (Maven Central) visa
		     Boot 3.4.x: DataSourceObservationAutoConfiguration referencia 4 classes que
		     mudaram de pacote no module-split do Boot 4 (ObservationAutoConfiguration,
		     ObservationRegistryCustomizer, MicrometerTracingAutoConfiguration,
		     DataSourceAutoConfiguration) e usa @ConditionalOnEnabledTracing, removida sem
		     substituto no Boot 4 — NoClassDefFoundError/ClassNotFoundException ao subir a
		     aplicação. 1.1.1-boot4fix corrige só essa classe (recompilada com os pacotes certos
		     do Boot 4.1.0, anotação removida — redundante nesta stack, tracing sempre
		     habilitado), resto do jar oficial idêntico (só as 5 classes de
		     DataSourceObservationAutoConfiguration/nested foram recompiladas e substituídas no
		     jar, via decompilação CFR — mesmo processo usado para o fix do lib-fintech-log).
		     Instalada apenas no repositório Maven LOCAL desta máquina. Quando o projeto
		     jdbc-observations publicar uma versão com suporte a Boot 4.x, trocar para a versão
		     real. -->
		<dependency>
			<groupId>net.ttddyy.observation</groupId>
			<artifactId>datasource-micrometer-spring-boot</artifactId>
			<version>1.1.1-boot4fix</version>
		</dependency>

	</dependencies>

	<build>

		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</path>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok-mapstruct-binding</artifactId>
							<version>${lombok-mapstruct-binding.version}</version>
						</path>
						<path>
							<groupId>org.mapstruct</groupId>
							<artifactId>mapstruct-processor</artifactId>
							<version>${mapstruct.version}</version>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>
		</plugins>
		<finalName>${project.artifactId}</finalName>
	</build>
</project>